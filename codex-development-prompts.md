# Codex 后续开发提示词

## 当前项目基线

- 本地平台：macOS/Darwin arm64，shell 为 `zsh`，项目路径为 `/Users/a111/chen/code/ly`。
- 技术栈：Vite + React + TypeScript，现有命令为 `npm run dev`、`npm run build`、`npm run preview`。
- 当前状态：已有游客端、运营端、商户端多页面前端原型，数据主要来自 `src/data/mockData.ts`。
- 可用真实数据：`poi-data/` 下已有 11616 条可用 POI 数据、城市数据、来源审计和校验文档。
- 构建基线：`npm run build` 当前通过，但存在单 chunk 超 500 kB 的性能提示。

## 使用方式

建议按阶段把下面提示词逐条交给 Codex。每个阶段完成后，都要求 Codex 输出：

- 变更范围
- 修改文件
- 验证命令与结果
- 未完成事项
- 下一阶段建议

如需让 Codex 一次性连续推进，可先使用「主控提示词」，再补充「阶段提示词」。

---

## 主控提示词

```markdown
你是高级产品经理 + 全栈工程师，正在接手 `/Users/a111/chen/code/ly` 项目。请先完整阅读项目结构、`deep-research-report.md`、`package.json`、`src/`、`poi-data/README.md`、`poi-data/VALIDATION.md`，再推进后续开发。

项目目标：把当前“生活智能体文旅体”前端演示原型，推进为可运行、可演示、可验收的 MVP 系统。系统需要覆盖游客端、运营端、商户端，重点完成真实 POI 数据接入、领域模型、API/服务层、AI 旅行助手、票务预约、行程规划、运营看板、权限角色、测试与部署。

执行原则：
1. 先审查现状，再给 3-7 步实施计划。
2. 低风险变更可直接做；涉及后端架构、数据库 Schema、接口契约、权限、订单/支付流程等高风险变更，先给计划和影响范围。
3. 优先复用现有 React/Vite/TypeScript 结构，避免无必要重构。
4. 逐步替换 `src/data/mockData.ts`，不要一次性破坏所有页面。
5. `poi-data/` 中真实 POI 数据优先用于推荐、搜索、详情、地图候选召回。
6. AI 能力先做 provider-agnostic adapter，通过环境变量配置模型服务；没有密钥时保留本地 deterministic fallback，确保演示可运行。
7. 每个阶段结束必须执行 `npm run build`；如新增测试，执行对应测试命令。
8. 不得删除用户已有改动；如发现脏工作区或不相关变更，只说明并避开。

请现在开始第 0 阶段：项目审计与 MVP 缺口分析。输出可执行路线图，不要先写大段理论。
```

---

## 阶段 0：项目审计与任务拆解

```markdown
请对 `/Users/a111/chen/code/ly` 做一次工程审计。

必须检查：
- `package.json`、`vite.config.*`、`tsconfig*.json`
- `src/App.tsx`
- `src/pages/TravelPages.tsx`
- `src/pages/AdminPages.tsx`
- `src/components/common.tsx`
- `src/data/mockData.ts`
- `src/types/index.ts`
- `poi-data/README.md`
- `poi-data/VALIDATION.md`
- `deep-research-report.md`

输出：
1. 当前系统已经完成的能力。
2. 仍停留在静态演示/mock 的能力。
3. MVP 必须补齐的功能清单，按游客端、运营端、商户端、技术底座分组。
4. 推荐技术路线：前端服务层、后端/API、数据存储、AI adapter、测试、部署。
5. 3-7 个开发阶段，每阶段给验收标准。

限制：
- 本阶段只做审计和路线图，除非发现显而易见的一行级错误，否则不要修改代码。
- 所有结论必须引用本地文件证据。
```

---

## 阶段 1：产品范围与验收标准

```markdown
请基于 `deep-research-report.md` 和当前页面路由，为系统补充一份 MVP 产品规格说明。

交付物：
- 新增或更新一份 Markdown 文档，建议路径为 `docs/mvp-spec.md`；如果没有 `docs/`，请创建。
- 内容包括：产品定位、目标用户、核心用户故事、MVP 范围、不做范围、页面清单、数据对象、关键流程、验收标准。

必须覆盖：
- 游客端：首页、AI助手、推荐、行程规划、景点详情、票务预约、支付/订单、我的行程、套餐、沉浸体验、地图。
- 运营端：看板、内容/商户管理、知识库、活动专题、审核、工作流。
- 商户端：商户工作台、库存/订单/营业状态/审核状态。

要求：
- 写清楚每个模块的输入、输出、核心状态、失败状态。
- 给出可验收标准，例如“搜索杭州 POI 能返回真实数据”“没有 LLM key 时 AI 助手仍能返回兜底方案”。
- 不要编造已接入的第三方能力；未接入的标注为后续接口。
```

---

## 阶段 2：领域模型与真实 POI 数据接入

```markdown
请把当前前端从纯 `mockData` 推进到领域数据服务层。

目标：
- 建立统一类型和服务层，让页面逐步使用真实 POI 数据。
- 优先接入 `poi-data/usable-pois.json`、`poi-data/usable-pois-by-city.json`、`poi-data/china-prefecture-cities.json`。

建议实现：
- 新增 `src/services/poiService.ts`
- 新增或扩展 `src/types/index.ts` 中的 `Poi`、`City`、`PoiCategory`、`PoiSearchParams`
- 新增搜索、城市筛选、分类筛选、详情查询、推荐候选召回函数
- 页面先替换推荐页、景点详情页、地图页中最明显的静态 POI 列表

要求：
- 保留必要的演示数据，但明确命名为 fallback/demo，不再混同真实 POI。
- 对缺失字段做安全处理，例如图片、评分、价格、开放时间为空时页面不能崩。
- 完成后执行 `npm run build`。

输出：
- 修改文件列表
- 已接入真实 POI 的页面
- 仍使用 mock 的页面和原因
```

---

## 阶段 3：API 与后端最小闭环

```markdown
请为当前项目设计并实现最小后端/API 闭环。

先检查项目是否已有后端。如果没有，请采用与现有 Node/Vite 工程兼容、改动最小的方案：
- 可在 `server/` 下新增 TypeScript Node API；
- 数据存储优先选择 SQLite 或本地 JSON seed，避免一开始引入过重基础设施；
- API 要能被前端本地开发环境调用。

必须实现的 API：
- `GET /api/health`
- `GET /api/pois`
- `GET /api/pois/:id`
- `GET /api/cities`
- `POST /api/itineraries/generate`
- `GET /api/tickets/options`
- `POST /api/orders`
- `GET /api/admin/metrics`

要求：
- 先给接口契约和数据模型计划，再实现。
- 加入基础错误处理和输入校验。
- 前端新增 API client，替换至少 2 个页面的数据来源。
- 保留无后端时的 fallback，保证演示可运行。
- 更新 `package.json` 脚本，例如 `dev:client`、`dev:server`、`dev` 或说明如何启动。
- 执行构建/类型检查。
```

---

## 阶段 4：AI 旅行助手与 Agent 编排

```markdown
请实现 AI 旅行助手的最小可用版本。

目标：
- 将 `AIChat` 从固定回复升级为真实服务流程：意图识别、POI 检索、票务/路线工具调用、答案生成。
- 没有模型 API key 时，也必须有可演示 fallback。

实现要求：
- 新增 AI adapter，不绑定单一厂商；通过环境变量配置 provider、baseURL、apiKey、model。
- 新增工具函数：POI 搜索、票务库存查询、行程候选生成、路线提示。
- 回复必须包含：回答文本、推荐卡片、工具调用状态、置信度/来源提示。
- 对票务、价格、开放时间等易变信息提示“以官方接口为准”。
- 加入超时、失败、空结果处理。

验收：
- 用户输入“西湖一日游，带老人，少排队”，能返回结构化行程建议。
- 用户输入“帮我预约雷峰塔上午票”，能给出票务候选和确认动作，而不是直接假装支付成功。
- 无 API key 时页面仍可演示。
- 执行 `npm run build`。
```

---

## 阶段 5：票务、订单与支付演示闭环

```markdown
请完成票务预约、订单、支付演示闭环。

范围：
- 票务选项、时段库存、游客信息、订单确认、支付状态、电子凭证、我的订单。

要求：
- 不接真实支付；实现 mock payment provider，并明确这是演示支付。
- 订单状态至少包括：待支付、已支付、待出行、已核销、已取消、退款中。
- 支持失败状态：库存不足、时段不可用、游客信息缺失、支付超时。
- 前端页面之间要能传递或读取订单状态，刷新后尽量不丢失，可使用 API 或 localStorage fallback。
- 更新类型定义和服务层，减少页面内硬编码。
- 执行 `npm run build`。
```

---

## 阶段 6：运营后台与商户端

```markdown
请把运营后台和商户端从静态展示推进到可操作状态。

优先模块：
- 运营看板：从 API 获取指标、客流、咨询热点、告警。
- 内容/商户管理：列表查询、筛选、创建/编辑、审核状态。
- 知识库：FAQ 列表、分类、发布状态、命中次数。
- 活动专题：活动列表、上下架状态、CTR/PV/UV 指标。
- 审核中心：待审核、通过、驳回、风险提示。
- 商户工作台：营业状态、库存同步、订单列表。

要求：
- 每个操作要有成功/失败状态，不只改按钮样式。
- 关键表单要做基础校验。
- 把公共表格、筛选、状态标签逻辑抽出来复用。
- 不要引入复杂状态管理库，除非能证明已有代码需要。
- 执行 `npm run build`。
```

---

## 阶段 7：权限、角色与安全边界

```markdown
请为系统补齐最小权限模型。

角色：
- 游客 visitor
- 运营人员 operator
- 审核人员 reviewer
- 商户 merchant
- 管理员 admin

要求：
- 增加登录/角色切换的演示入口，真实认证可后续接入。
- 路由层做访问控制：游客不能进入运营后台，商户只能进入商户工作台，管理员可访问全部。
- API 层预留鉴权中间件或 mock auth middleware。
- 对敏感操作加入确认，例如删除、驳回、下架。
- 不要在前端假装有真实安全能力；文档中标明当前是演示鉴权。
- 执行 `npm run build`。
```

---

## 阶段 8：测试、性能与工程质量

```markdown
请补齐工程质量保障。

任务：
- 增加测试方案，优先覆盖纯函数、服务层、API client、关键组件状态。
- 如项目没有测试框架，选择与 Vite/React/TypeScript 兼容的轻量方案。
- 解决或解释构建中 chunk 超 500 kB 的提示，优先考虑路由级 lazy loading 和图表库拆分。
- 检查图片加载失败、空数据、移动端布局、长文本溢出。
- 保证 `npm run build` 通过。

输出：
- 新增测试命令
- 测试覆盖范围
- 性能优化点
- 仍需后续处理的风险
```

---

## 阶段 9：部署与演示材料

```markdown
请为项目补齐部署和演示交付。

交付物：
- `README.md` 或更新现有说明：项目介绍、启动方式、环境变量、演示账号、功能清单。
- `.env.example`：列出 API、AI provider、地图/天气等配置项，不写真实密钥。
- 部署说明：本地开发、生产构建、静态部署、后端部署。
- 演示脚本：按游客端、运营端、商户端给 5-8 分钟演示路径。
- 验收清单：每个核心功能对应检查步骤。

要求：
- 所有命令必须在本地验证。
- 不要承诺未接入的真实第三方接口。
- 执行 `npm run build`，如有测试也执行测试。
```

---

## 一次性执行版提示词

如果你希望 Codex 尽量连续做完整系统，可使用下面这段：

```markdown
你是高级产品经理 + 全栈工程师，请接手 `/Users/a111/chen/code/ly`，把当前“生活智能体文旅体”半成品推进为可运行 MVP。

请按以下阶段连续推进，但每遇到高风险决策先输出计划和影响范围：
1. 审计项目，确认当前完成度和缺口。
2. 补充 MVP 产品规格和验收标准。
3. 建立领域模型和服务层，优先接入 `poi-data/` 真实 POI。
4. 如果没有后端，新增最小 Node/TypeScript API，支持 POI、城市、行程、票务、订单、运营指标。
5. 改造 AI 旅行助手，做 provider-agnostic AI adapter；无 key 时保留 fallback。
6. 完成票务预约、订单、支付演示闭环。
7. 完成运营后台、知识库、审核、商户端可操作状态。
8. 增加最小权限模型和路由保护。
9. 增加测试、性能优化、路由级代码拆分。
10. 补齐 README、`.env.example`、部署说明和演示脚本。

硬性要求：
- 每阶段尽量小步提交式修改，不做无关重构。
- 所有页面必须有空数据、加载、错误状态。
- 所有关键服务必须有类型定义和错误处理。
- 不删除用户已有改动。
- 每阶段结束运行 `npm run build`；如新增测试，也运行测试命令。
- 最终输出修改文件、验证结果、剩余风险和下一步建议。

现在从项目审计开始执行。
```
